% A template for papers so it's harder for me to put off writing things
% You'll want a folder called images

\documentclass{article}
% Memoir is an alternative, can be real customisable
% \documentclass[oneside]{memoir}

% Refer to a bunch of colours 
\usepackage{xcolor}
% Allow pictures
\usepackage{graphicx}
    % Put images in this subfolder
    \graphicspath{{./images/}}
\usepackage{hyperref}
    \hypersetup{
        colorlinks = true,
        citecolor = blue,
        urlcolor = blue,
        linkcolor = black
    }
\usepackage[margin=1.75in]{geometry}
% Not needed with LuaTeX
\usepackage[utf8]{inputenc}
% Let Latex not die with weird characters
\usepackage[T1]{fontenc}
% Gorgeous kerning
\usepackage{microtype}
% Nice control over list things, their spacing etc
\usepackage{enumitem}
% Pretty tables
\usepackage{booktabs}
    % Apparently this is how you remove spacing to the vertical edges
    % \begin{tabular}{@{}lll@{}}
    % You'll want to use toprule, midrule, bottomrule
% Table with fill columns
\usepackage{tabularx}

% Referencing. You'll likely want to set bibstyle and citestyle
\usepackage{biblatex}
    % Do make this file, you'll need it
    \addbibresource{references.bib}

\title{}
\author{}
% \date{}

\begin{document}
    % More space between rows: 
    \renewcommand{\arraystretch}{1.2}

    % This lets me happily edit the text and not care about the style until conference
    \input{content.tex}
    \printbibliography
\end{document}
